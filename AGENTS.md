# artobsidian-lab — 공통 지침

같은 목표(미술 자료용 개인 위키 앱)를 두 사람이 각자 다른 AI로 독립 구현하고, 과정과 결과를 비교하며 배우는 실험의 upstream 레포.
참가자: 개발자(Claude Code, Fable 5) / 디자이너(Codex, GPT-sol). 이 파일이 규칙 원본이고, CLAUDE.md는 이 파일을 import한다.

## 실험 구조

- 이 레포(upstream)에는 공통 뼈대와 스펙, 회고 로그만 둔다. 구현 작업물은 올리지 않는다.
- 각자 이 레포를 포크해서 독립 진행. 서로의 포크는 체크포인트 전까지 보지 않는다.
- 회고 로그는 upstream에 PR로 올려 공유한다 (파일: `docs/ai-feedback/YYYY-MM-DD-이름-주제.md`, 양식: `docs/ai-feedback/TEMPLATE.md`).

## AI 에이전트 팀 구성 (각자 포크에서 운영)

| 팀 | 담당 | 산출물 위치 |
|---|---|---|
| 리서치/기획팀 | 시장조사, 경쟁사 분석, 사용자 페인포인트, 문제-솔루션-타겟, 기능 우선순위, 로드맵 | `docs/research/`, `docs/prd/` |
| 디자인팀 | 트렌드 리서치, 디자인 제안, 브리프, 에셋 정리 | `design/` |
| 개발팀 | 구현, 스펙 문서, 기술 검토 | 소스 폴더, `docs/specs/` |
| 검증팀 | 피처별 QA 체크리스트, 합격 기준, 검증 프로세스, 피드백 | `qa/` |

## 공통 규칙

- 모든 문서는 마크다운, 상단에 날짜(절대 날짜)와 버전 명시
- 개발은 `docs/prd/` 최신 기획서 기준으로만 진행 — 기획서에 없는 기능 임의 추가 금지
- 검증팀은 기능별 합격 기준을 `qa/checklist.md`에 **먼저** 정의한 후 검증
- 레퍼런스 링크는 `references.md`에 링크 + 한 줄 요약으로 누적
- 데이터 포맷 규약: 노트는 평문 `.md`, 옵시디언 볼트 호환 — 구현이 달라도 최종 이식이 가능하게
- 작업 세션이 끝나면 회고 로그 작성. AI가 0~4번(도구, 프롬프트 원문, 시행착오)을 초안으로 채우고, 5번 회고는 본인이 작성

## 시작 전 필수 (포크하기 전에 둘이 합의)

- `docs/spec.md`에 기능 목록, 단계별 산출물 정의, 합격 기준을 확정할 것. 스펙이 계약이다 — 이게 없으면 결과 차이의 원인을 분석할 수 없다.

## 머지 전략 (미정 — 멘토 상의 후 확정)

- 후보 ①: 단계(기획/디자인/개발)별로 더 나은 쪽을 채택해 머지
- 후보 ②: 끝까지 따로 + 단계 경계마다 회고 로그만 교환하는 체크포인트, 머지는 최종 1회
- 확정되면 이 섹션을 규칙으로 교체할 것
